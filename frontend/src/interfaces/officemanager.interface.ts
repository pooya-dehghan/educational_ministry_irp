export interface OfficeManagerInterface {
  id: number;
  username: string;
  national_code: string;
  phone_number: string;
  email: string;
  first_name: string;
  last_name: string;
  birthday_date: Date;
  gender: string;
  // is_active: boolean;
  // created_date: Date;
  // updated_date: Date;
  // avatar: string;
  // is_admin: boolean;
  personal_code: string;
  region: number;
}
