import { makeStyles } from '@mui/styles';

export const useTextFieldStyles = makeStyles((theme) => ({
  label: {
    fontSize: '16px',
    fontWeight: 'bold',
    color: '#333',
    // Add more label-specific styling as needed
  },
}));
